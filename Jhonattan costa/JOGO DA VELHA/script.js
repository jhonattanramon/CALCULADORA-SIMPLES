const divs = document.querySelectorAll(".tracos")
console.log(divs)
const test = (caseNumber) => {
  const zero = "o"
  const xis = "x"


  if( divs[caseNumber] == zero || divs == xis){
    return alert('test')
  }else if(divs == " "){
    divs[]
  }
  

}